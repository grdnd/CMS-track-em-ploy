// import modules
const { prompt } = require("prompt");

// import database
const db = require("./db");

// create a function to initialize application
function init() {
  menu();
}

function menu() {}

init();
